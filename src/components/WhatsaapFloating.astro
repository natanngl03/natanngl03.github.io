---
import { Icon } from "astro-icon/components";
---

<a
    class="btn text-white wa-btn"
    href="https://wa.me/6285117436727?text=Hallo%20erosoft.id%2C%20saya%20ingin%20bertanya%20mengenai%20pembuatan%20website."
    id="waBtn"
>
    <p class="m-0">Hubungi Kami</p>
    <Icon name="fa6-brands:whatsapp" size={20} />
</a>

<style>
    .wa-btn {
        background-color: var(--bs-teal);
        position: fixed;
        bottom: 40px;
        right: 10px;
        z-index: 99;
        border-radius: 50px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        padding: 10px 20px;

        /* Mulai di luar layar */
        transform: translateX(120%);

        /* Slide masuk 2 detik, lalu pulse terus */
        animation:
            slideInRight 2s ease-out forwards,
            pulse-shadow 2s infinite 2s;
    }

    @keyframes slideInRight {
        from {
            transform: translateX(120%);
            opacity: 0;
        }

        to {
            transform: translateX(0);
            opacity: 1;
        }
    }

    @keyframes pulse-shadow {
        0% {
            box-shadow: 0 0 0 0 rgba(59, 246, 75, 0.6);
        }

        70% {
            box-shadow: 0 0 0 12px rgba(59, 246, 75, 0);
        }

        100% {
            box-shadow: 0 0 0 0 rgba(59, 246, 75, 0);
        }
    }
</style>
