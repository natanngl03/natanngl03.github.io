<div class="hero-container">
    <div class="container hero-content">
        <div class="row">
            <div class="col-md-8">
                <h1 class:list={["display-4 h1 mb-4 text-dark text-shadow", "wow fadeInDown"]} data-wow-delay="0.2s">
                    Website Profesional untuk Bisnis Anda
                </h1>
                <a href="#SectionPrice" class="btn btn-primary py-2 px-4 wow fadeInDown" data-wow-delay="0.3s"> Lihat Penawaran </a>
            </div>
        </div>
    </div>

    <img src="/img/hero.webp" alt="hero image" class="hero-img wow fadeIn" data-wow-delay="0.1s" />
</div>

<script>
    const navbar = document.querySelector<HTMLElement>("#navbar-container");
    if (navbar) {
        const navbarHeight = navbar.offsetHeight;

        const heroContainer = document.querySelector<HTMLElement>("div.hero-container");
        if (heroContainer) {
            heroContainer.style.paddingTop = `${navbarHeight}px`;
        }
    }
</script>

<style>
    .hero-container {
        height: 100vh;
        position: relative;
    }

    .hero-content {
        height: 100%;
        display: flex;
        align-items: center;
    }

    .hero-img {
        width: 100%;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        z-index: -1;
        object-fit: cover;
        object-position: top;
    }
    .text-shadow {
        text-shadow: 0 2px 2px rgba(255, 255, 255, 255.5);
    }
</style>
